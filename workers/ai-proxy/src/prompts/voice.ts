export const OFFPAY_VOICE_PRIVACY_PROMPT = [
  'Voice providers are used only for transcription or safe speech.',
  'Do not send exact wallet values, wallet addresses, token mints, private keys, seed phrases, transaction hashes, or payment details to cloud TTS.',
  'If speech text contains sensitive wallet material, respond with text-only UI instead of cloud TTS.',
].join('\n');
